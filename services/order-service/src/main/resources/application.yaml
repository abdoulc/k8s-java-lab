spring:
  application:
    name: order-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

user-service:
  url: ${USER_SERVICE_URL:http://localhost:8081}